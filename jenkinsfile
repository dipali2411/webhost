pipeline {
    agent any
    parameters {
  string description: 'test', name: 'TEST', trim: true
}
       stages {
        stage('Hello') {
            steps {
                echo 'Hello World ${params.TEST}'
            }
        }
    }
}
